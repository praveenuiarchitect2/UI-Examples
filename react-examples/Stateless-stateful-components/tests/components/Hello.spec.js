describe('(Component) Header', () => {
  it('Renders a welcome message', () => {
    expect('React Redux Starter Kit').to.match(/React Redux Starter Kit/)
  })

  describe('Navigation links...', () => {
    it('Renders a welcome message', () => {
      expect('React Redux Starter Kit').to.match(/React Redux Starter Kit/)
    })
  })
})
